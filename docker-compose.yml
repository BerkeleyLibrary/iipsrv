services:
  app:
    build:
      context: .
      target: production
    init: true
    volumes:
      - ./artifacts:/opt/app/artifacts
    environment:
      - VERBOSITY=6
      - CORS=*
    ports:
      - 80:80
    networks:
      default:
        aliases:
          - iipsrv.test
